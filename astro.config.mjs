// @ts-check
import { defineConfig } from 'astro/config';

// Public teaching platform. Static build deployed to Cloudflare Pages.
// https://astro.build/config
export default defineConfig({
  site: 'https://biochempedia.deppmannlab.com',
});
